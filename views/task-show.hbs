<div class="container"> {{#if task}}
    <h3> Task: {{task.tOrder}} - {{task.tTitle}}</h3>
    <h4>Description:</h4>
    <h5 class="well well-sm">{{task.tDesc}}</h5>
    <h4>Importance:</h4>
    <h5 class="well well-sm">{{task.importance}}</h5>
    <h4>Task Points:</h4>
    <h5 class="well well-sm">{{task.points}}pts</h5>
    <h4>Due Date:</h4>
    <h5 class="well well-sm">{{task.dueDate}}</h5>
    <h4>Task Status:</h4>
    <h5 class="well well-sm">{{task.status}}</h5>
    {{else}}
    <h3>That task couldn't be found!</h3>
    {{/if}}
</div>

<p>Title: {{bucket.bTitle}} - {{task.tTitle}}</p>
<div class="container">
    <form action="/bucket/{{bucket.bTitle}}/{{task._id}}?_method=delete" method="post">
        <div class="form-group row">
            <div class="offset-sm-2 col-sm-10">
                <button type="submit" class="btn btn-danger">Remove</button>
            </div>
        </div>
    </form>
</div>